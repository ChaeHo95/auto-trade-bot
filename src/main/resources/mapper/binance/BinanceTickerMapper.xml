<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.autotradebot.mapper.binance.BinanceTickerMapper">

    <!-- ✅ Ticker 데이터 저장 (선물 필드 추가) -->
    <insert id="insertTicker">
        INSERT INTO binance_ticker (symbol, event_time, price_change, price_change_percent, weighted_avg_price, last_price, open_price, high_price, low_price, volume)
        VALUES (#{symbol}, #{eventTime}, #{priceChange}, #{priceChangePercent}, #{weightedAvgPrice}, #{lastPrice}, #{openPrice}, #{highPrice}, #{lowPrice}, #{volume})
            ON DUPLICATE KEY UPDATE
                                 price_change = VALUES(price_change),
                                 price_change_percent = VALUES(price_change_percent),
                                 weighted_avg_price = VALUES(weighted_avg_price),
                                 last_price = VALUES(last_price),
                                 open_price = VALUES(open_price),
                                 high_price = VALUES(high_price),
                                 low_price = VALUES(low_price),
                                 volume = VALUES(volume)
    </insert>

    <!-- ✅ 특정 심볼의 최신 Ticker 데이터 가져오기 -->
    <select id="getLatestTicker" resultType="com.example.autotradebot.dto.binance.BinanceTickerDTO">
        SELECT * FROM binance_ticker
        WHERE symbol = #{symbol}
        ORDER BY event_time DESC
            LIMIT 1
    </select>

</mapper>
